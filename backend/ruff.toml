line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B", "UP", "C4", "SIM"]
ignore = ["B008"]  # FastAPI Depends() in defaults is intended
[lint.per-file-ignores]
"tests/*" = ["S101"]  # asserts are fine in tests
