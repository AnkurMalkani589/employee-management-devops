# Ansible inventory.
# Copy to inventory.ini and fill in the host from `terraform output app_public_ip`.
#
#   [app]
#   <APP_PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/your-key.pem

[app]
# 203.0.113.10 ansible_user=ubuntu

[app:vars]
ansible_python_interpreter=/usr/bin/python3
app_directory=/home/ubuntu/employee-management-devops
